name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets --locked -- -D warnings

  test:
    runs-on: ubuntu-latest
    # The same Debian release as the container image, so media tests run
    # against the vips and ffmpeg that ship.
    container: rust:1.98-trixie
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: uwuu
          POSTGRES_PASSWORD: uwuu
          POSTGRES_DB: uwuu
        options: >-
          --health-cmd "pg_isready -U uwuu"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      # sqlx::test creates a throwaway database per test on this server.
      DATABASE_URL: postgres://uwuu:uwuu@postgres:5432/uwuu
    steps:
      - uses: actions/checkout@v5
      - name: Install media tools
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            libvips-tools libheif-plugin-dav1d libheif-plugin-aomenc ffmpeg
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace --locked

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm run build
      # The bundle is committed so building the binary needs no Node.
      - name: Committed bundle matches the sources
        run: git diff --exit-code -- ../crates/web/static/js

  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@1.94
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --workspace --all-targets --locked

  deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: EmbarkStudios/cargo-deny-action@v2

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: docker build --tag uwuubooru:ci .
